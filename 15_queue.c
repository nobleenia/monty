#include "monty.h"

/**
 * queue - sets the stack operation mode to queue (FIFO)
 * @stack: a pointer to the pointer to the stack
 * @line_num: current line number being processed
 * Return: Nothing
 */
void queue(stack_t **stack, unsigned int line_num)
{
UNUSED(stack);
UNUSED(line_num);
args->stack = 0;
}
