#include "monty.h"

/**
 * stack - set the stack operation mode to stack (LIFO)
 * @stack: pointer to the pointer to the stack
 * @line_num: current line number being processed
 *
 * Return: Nothing
 */
void stack(stack_t **stack, unsigned int line_num)
{
UNUSED(stack);
UNUSED(line_num);
args->stack = 1;
}
